buildscript {

    apply from: 'dependencies.gradle'

    repositories {
        google()
        jcenter()
        mavenLocal()
        mavenCentral()
        maven {
            url "https://jitpack.io"
        }
    }

    dependencies {
        classpath classPaths.gradlePath
        classpath classPaths.kotlinVersion
//        classpath 'com.github.hoanganhtuan95ptit.AutoService:autobind-plugin:1.0.2'
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        mavenLocal()
        mavenCentral()
        maven {
            url "https://jitpack.io"
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
